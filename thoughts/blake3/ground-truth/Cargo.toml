[workspace]

[package]
name = "gt"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = { version = "1.8.5", default-features = false, features = ["std", "pure"] }

[profile.dev]
debug = false
