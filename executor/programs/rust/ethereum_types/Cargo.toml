[workspace]

[package]
name = "ethereum_types"
version = "0.1.0"
edition = "2024"

[dependencies]
ethereum-types = { version = "0.15.1", default-features = false}
lambda-vm-syscalls = { path = "../../../../syscalls" }
