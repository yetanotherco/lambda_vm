[workspace]

[package]
name = "serde"
version = "0.1.0"
edition = "2024"

[dependencies]
lambda-vm-syscalls = { path = "../../../../syscalls" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
