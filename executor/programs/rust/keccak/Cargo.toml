[workspace]

[package]
name = "keccak"
version = "0.1.0"
edition = "2024"

[dependencies]
lambda-vm-syscalls = { path = "../../../../syscalls" }
tiny-keccak = { version = "2.0", features = ["keccak"] }
